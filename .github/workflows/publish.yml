name: Release to PyPI

on:
  create:
    tag:

jobs:
  build-n-publish:
    name: Build and publish coh_piggs_extractor to PyPI
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

